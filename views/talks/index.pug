//- Talks Index Page (Inbox/Messaging)
//- Converted from app/views/talks/index.html.slim

//- Set layout classes
- var userLayoutClass = 'no-padding-xs'
- var contentClass = 'no-padding-xs'

//- Custom styles for full-height layout
- content_for('modals', function() {
  style.
    html, body {
      height: 100%;
      min-height: 70vh;
    }
- })

//- Hide logo on mobile
if browser.device.mobile
  - content_for('logo_placeholder', function() {
    //- nil (empty)
  - })

//- Bottom scripts for Vue component
- content_for('bottom_scripts_section', function() {
  script(src='https://cdnjs.cloudflare.com/ajax/libs/file-uploader/5.15.3/fine-uploader.min.js')

  if showWebPush
    != render('layouts/onesignal')

  script.
    simbi('createComponent').then(function(createComponent) {
      createComponent('InboxContainer', {
        el: '#main-wrapper',
        state: gon.initialState
      });
    });
- })
